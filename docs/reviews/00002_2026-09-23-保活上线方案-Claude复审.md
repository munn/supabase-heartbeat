# 保活上线方案独立评审

日期：2026-09-23
引擎：Claude Code Opus（独立只读评审）
状态：方案及一次性凭据提交脚本均获 Claude `PASS`；用户已到节点确认，生产配置已完成；首次真实定时执行已验证。

## 对象

两个现有 Supabase 项目：Data API 已开启的项目走现有 `keepalive()` RPC；Data API 关闭的项目走现有 Worker 的 PostgreSQL/Hyperdrive 分支。Cloudflare 账号目前没有本 Worker 与 Hyperdrive。复用仓库现有代码，不增加定时系统或重试机制。

## 评审收敛

- 首轮 `FIX`：确认 Session pooler 实际地址和证书链、选择已开启 Data API 库的 SQL 应用路径、隔离数据库密码、按两个库分别验收真实查询。逐项完成只读核查并修订执行步骤。
- 第二轮 `FIX`：删除不能证明生产效果的预览执行；改用首次真实 Cron 验收；显式启用 Workers Logs；禁止密码出现在命令行参数；使私有 Wrangler 配置处于 Git 忽略目录并完整复制入口和触发设置。
- 第三轮 `FIX`：进一步将密码改为单进程内存生成，并通过 HTTPS 请求体直接交给 Cloudflare API；Supabase 只收到 SCRAM-SHA-256 verifier。修正私有配置中的相对入口路径。
- 最终 Claude 回执：**PASS**，无剩余必修项。结论覆盖方案符合性、实现质量、验证力度与 YAGNI。其回执中的“同侧 fallback”自标与实际调用不符；本次由当前 Codex 会话调用独立 Claude Code CLI，属于跨引擎独立评审。

## 已取得的证据

- 已核对 Data API 关闭库的 Session pooler 地址，并在局域网服务器验证 Supabase CA 对其 TLS 证书和主机名校验成功。
- 只读数据库查询确认：已开启 Data API 库没有同名函数，`anon` 可用 `public` schema；关闭 Data API 库没有目标角色，`pg_stat_statements` 扩展已启用。
- 局域网服务器对私有 Wrangler 配置执行 `wrangler deploy --dry-run` 成功，能识别 Hyperdrive 绑定。真实 ID 创建后需再次执行 dry-run。
- 现有 Worker 的 TypeScript 检查及 13/13 测试此前已在局域网服务器通过。本轮未改源代码。

## 上线与验收边界

先取得用户对生产 SQL、角色及凭据、Cloudflare OAuth 权限、CA/Hyperdrive、Worker secret 和部署的到节点明确确认。部署时先 Worker 再 secret；首次真实 Cron 后以 Workers Logs 的两个目标 `OK`（RPC 目标需有 HTTP 200）和 A 库按专用角色的 `SELECT now()` 统计证明查询到达，必要时再核 B 库 RPC API 日志。若尚未发生真实 Cron，只报告“已配置，未完成运行验收”。Supabase 对免费项目的暂停判定并无固定请求次数保证。

## 2026-09-23 生产执行回读

- 用户对上述范围明确确认后，Cloudflare Wrangler OAuth 获得已披露的账号、Workers 与证书权限；先只读核对 Hyperdrive 和证书访问。
- B 库应用仓库已有 SQL 并用现有 legacy anon key 实测 RPC 返回 HTTP 200。回读显示 `anon` 与 `service_role` 有函数执行权，`authenticated` 无执行权。
- A 库使用一次性脚本生成密码：Supabase 只接收 SCRAM verifier，Cloudflare Hyperdrive 接收 HTTPS 请求体中的密码。Claude 对实际脚本独立复审最终 `PASS`。回读确认角色无超级用户、建库、建角色、复制、绕过 RLS 或继承权限，连接上限 5、语句超时 5 秒。
- Hyperdrive 回读：Session pooler 地址和专用角色正确，缓存关闭，CA 加 `verify-full`，源站连接上限 5。
- 真实 Hyperdrive ID 写入 Git 忽略的私有 Wrangler 配置后，在局域网服务器上第二次 dry-run 通过。Worker 随后部署，两个目标写入 `SUPABASE_TARGETS` secret；Cloudflare 回读显示每天 UTC 01:00、09:00、17:00 的 Cron、持久化日志开启、secret 名存在，最新部署为 secret 更新版本。
- 截至 2026-09-23 21:09 UTC，A 库专用角色的 `SELECT now()` 统计调用数为 0。下一次真实 Cron 尚未发生，不能称两库已通过运行验收；本任务已设置只读自动跟进，在首次真实触发后核验并报告。

## 即时远程执行

用户随后要求两库现在各运行一次。使用 Wrangler 的 Cloudflare 远程预览和 `--test-scheduled`，将两目标配置仅注入临时预览进程，在 Cloudflare 网络触发同一 `scheduled()` 处理器。预览日志分别为关闭 Data API 的 PostgreSQL 目标 `OK` 和开启 Data API 的 RPC 目标 `HTTP 200`、`OK`。A 库专用角色的 `SELECT now()` 统计调用数从 0 增至 1，证明请求实际到达数据库。预览进程已结束；正式 Worker 的 Cron 配置未改。此证据证明两条连接路径即时可用，首次生产 Cron 的自动触发仍由前述跟进另行验收。

## 首次正式 Cron 验收

- Cloudflare Past Cron Events 显示 `0 1,9,17 * * *` 于 2026-09-24 01:01:05 UTC 执行，状态 `Success`。
- Workers Logs 在同一次定时调用（2026-09-24 01:01:06.644 UTC）记录 PostgreSQL 目标 `OK` 与 RPC 目标 `HTTP 200`、`OK`；该时间点的调用及两条日志均无错误。
- Supabase Management API 只读查询 A 库 `pg_stat_statements`：`heartbeat_probe` 角色的 `SELECT now()` 调用数由即时预览后的 1 增至 2，与首次正式 Cron 对应。
- 首轮真实定时保活通过；后续由生产 Worker 每天 UTC 01:00、09:00、17:00 继续执行。此验收只证明本次运行成功，不构成 Supabase 永不暂停的保证。

## 公开说明与集成复审

2026-09-24，Claude Code Opus 对拟公开的完整差异作只读复审，重点核对部署命令顺序、私有 Wrangler 配置、验收证据与信息暴露。首轮 `PASS` 并提出两项非阻断的说明改进：把 RPC 验收标准明确为 Worker 日志中的 HTTP 200，以及补充私有配置的 Git 忽略、入口相对路径、Cron 和日志设置。修订后的完整差异再次获得 `PASS`，无剩余阻断项；未包含生产 Hyperdrive ID、数据库密码、API key 或 Supabase 项目 ref。
