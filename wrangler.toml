# Supabase Heartbeat — generic free-tier keepalive worker
#
# A single worker keeps alive any number of Supabase projects. Targets are
# supplied via the SUPABASE_TARGETS secret (a JSON array), NOT hard-coded here,
# so adding/removing a project never requires a code change or redeploy of
# logic — only a secret update.
#
# See README.md for the full walkthrough.

name = "supabase-heartbeat"
main = "src/index.ts"
compatibility_date = "2025-03-16"
compatibility_flags = ["nodejs_compat"]

# Three queries daily. Supabase says a few user database requests per day are
# typically enough, but does not guarantee that any fixed schedule prevents pausing.
[triggers]
crons = ["0 1,9,17 * * *"]

# No KV / R2 / Queue / Services bindings — minimal attack surface.
# RPC targets use the SUPABASE_TARGETS secret. Add one Hyperdrive binding per
# Data API-disabled project, using the real configuration ID from Cloudflare:
# [[hyperdrive]]
# binding = "DB_NO_DATA_API"
# id = "YOUR_HYPERDRIVE_CONFIGURATION_ID"
