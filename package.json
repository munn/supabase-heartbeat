{
  "name": "supabase-heartbeat",
  "version": "0.1.0",
  "private": true,
  "description": "Generic Cloudflare Worker that keeps any number of Supabase free-tier projects alive via the keepalive() RPC.",
  "scripts": {
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.5.0"
  }
}
